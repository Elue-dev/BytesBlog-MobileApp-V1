import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  searchText: {
    textAlign: "center",
    marginBottom: 20,
    fontSize: 16,
  },
  subText: {
    fontStyle: "italic",
    fontWeight: "bold",
  },
});
